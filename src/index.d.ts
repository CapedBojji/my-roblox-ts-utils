import Grid from "Grid";
import Point2D from "Point";
import Segment from "Segment";
import GRect from "Rect";
import GeneralMath from "GeneralMath";

interface Utils {
    Grid: typeof Grid;
    Point2D: typeof Point2D;
    Segment: typeof Segment;
    GRect: typeof GRect;
    GeneralMath: typeof GeneralMath;
}

export = Utils; 
